# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ChoppedUp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ChoppedUp
	pod 'SwiftyJSON'
	pod 'Alamofire', '~> 4.0'

  target 'ChoppedUpTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ChoppedUpUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
